[Unit]
Description=Steam Big Picture Mode
After=multi-user.target

[Service]
Type=simple
User=root
Environment=XDG_RUNTIME_DIR=/run/user/__UID__
ExecStart=/usr/local/bin/start-steam-bigpicture.sh __USER__
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
